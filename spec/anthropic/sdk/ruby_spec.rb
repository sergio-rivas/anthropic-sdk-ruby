# frozen_string_literal: true

RSpec.describe Anthropic::Sdk::Ruby do
  it "has a version number" do
    expect(Anthropic::Sdk::Ruby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
